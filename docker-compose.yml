version: '3.8'

services:
  app:
    build: .
    container_name: reed-refactor-2
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped

